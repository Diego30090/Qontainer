#ifndef MAINVIEW_HPP
#define MAINVIEW_HPP

#include <QMainWindow>

class MainView : public QMainWindow
{
  Q_OBJECT
public:
  explicit MainView(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // MAINVIEW_HPP